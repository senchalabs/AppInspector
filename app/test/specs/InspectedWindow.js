StartTest(function (test) {

    //=================================================================
    test.diag("Ok assertions");

    test.ok(true, 'True is ok')
    test.ok(1, '1 is ok')
    test.ok({}, '{} is ok')
});