/**
 * @class   AI.model.Base
 * @extends Ext.data.Model
 */
Ext.define('AI.model.Base', {
    extend: 'Ext.data.Model'

    // we create a basic model as fields are now auto generated in Ext JS 5
});
